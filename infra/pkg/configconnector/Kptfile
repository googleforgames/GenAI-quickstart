apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: configconnector
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: simple setup for setting up config connector
pipeline:
  mutators:
  - image: gcr.io/kpt-fn/apply-setters:v0.1
    configPath: setters.yaml